<p style="color: green"><%= notice %></p>

<%= render @event %>

<div>
  <%= link_to "Editer", edit_event_path(@event) %> |
  <%= link_to "Retour", events_path %>

  <%= button_to "Supprimer l'événement", @event, method: :delete %>
</div>
